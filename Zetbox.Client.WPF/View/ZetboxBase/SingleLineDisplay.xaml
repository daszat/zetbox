<UserControl x:Class="Zetbox.Client.WPF.View.ZetboxBase.SingleLineDisplay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ctrls="clr-namespace:Zetbox.Client.WPF.CustomControls;assembly=Zetbox.Client.WPF.Toolkit">
    <DockPanel>
        <Image Source="{Binding Converter={StaticResource IconConverter}}"
               Width="14"
               Height="14" />
      <TextBlock Text="{Binding ObjectState, Converter={StaticResource ObjectStateToTextConverter}}"
                        Margin="3,0,0,0"
                        Visibility="{Binding ObjectState, Converter={StaticResource ObjectStateToTextVisibilityConverter}}" />
      <TextBlock Text="{Binding Name}"
                   Margin="3,0,0,0" />
    </DockPanel>
</UserControl>
