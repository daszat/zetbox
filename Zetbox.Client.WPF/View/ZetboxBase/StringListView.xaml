<ctrls:PropertyEditor x:Class="Zetbox.Client.WPF.View.StringListView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:ctrls="clr-namespace:Zetbox.Client.WPF.CustomControls;assembly=Zetbox.Client.WPF.Toolkit"
                     xmlns:view="clr-namespace:Zetbox.Client.WPF.View.ZetboxBase"
                     xmlns:kevin="clr-namespace:Microsoft.Samples.KMoore.WPFSamples.InfoTextBox;assembly=WPFSamplesLib"
                     xmlns:client="clr-namespace:Zetbox.Client.Presentables;assembly=Zetbox.Client">

    <DockPanel>
        <WrapPanel DockPanel.Dock="Top"
                   Orientation="Horizontal">
            <Button Click="AddNewHandler">Add new ...</Button>
            <Button Click="RemoveHandler">Remove selection from list</Button>
        </WrapPanel>

        <TextBox x:Name="PART_ItemEditBox"
                 DockPanel.Dock="Bottom"
                 Text="{Binding SelectedItemEditable, UpdateSourceTrigger=PropertyChanged}" />

        <ListView x:Name="lst"
                  ItemsSource="{Binding Value, Mode=OneWay}"
                  SelectedItem="{Binding SelectedItem}"
                  MinHeight="50"
                  MaxHeight="300" />
    </DockPanel>
</ctrls:PropertyEditor>
