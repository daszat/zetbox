@attribute [ViewDescriptor(Zetbox.App.GUI.Toolkit.Blazor)]

@if(ViewModel != null)
{
	<span>@ViewModel.FormattedValueAsync</span>
}

@code {
	[Parameter]
	public NullableDateTimePropertyViewModel ViewModel { get; set; }
}
