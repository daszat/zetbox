<UserControl x:Class="Kistl.Client.WPF.View.PropertyTextBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ctrls="clr-namespace:Kistl.Client.WPF.CustomControls"
             xmlns:view="clr-namespace:Kistl.Client.WPF.View"
             xmlns:kevin="clr-namespace:Microsoft.Samples.KMoore.WPFSamples.InfoTextBox;assembly=WPFSamplesLib"
             ToolTip="{Binding ToolTip}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition MinWidth="100"
                              MaxWidth="300"
                              SharedSizeGroup="Label" />
            <ColumnDefinition Width="*"
                              MinWidth="100"
                              MaxWidth="400"
                              SharedSizeGroup="Content" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0"
                   Text="{Binding Label}" />

        <kevin:InfoTextBox Grid.Column="1"
                           Text="{Binding Value, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=true}"
                           TextBoxInfo="{Binding ToolTip}" />
    </Grid>
</UserControl>