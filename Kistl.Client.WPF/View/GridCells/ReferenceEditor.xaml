<UserControl x:Class="Kistl.Client.WPF.View.GridCells.ReferenceEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <Button DockPanel.Dock="Right"
                Margin="1"
                Visibility="{Binding AllowNullInput, Converter={StaticResource BooleanToVisibilityConverter}}"
                IsEnabled="{Binding HasValue}"
                Click="ClearValueHandler">x</Button>

        <Button DockPanel.Dock="Right"
                Margin="1"
                Command="{Binding CreateNewItemAndSetValueCommand, Converter={StaticResource CommandConverter}}">+</Button>

        <Button DockPanel.Dock="Right"
                Margin="1"
                Command="{Binding SelectValueCommand, Converter={StaticResource CommandConverter}}">*</Button>

        <ContentPresenter Margin="3"
                          Content="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=true}"
                          ContentTemplateSelector="{StaticResource listItemTemplateSelector}" />

    </DockPanel>

</UserControl>
