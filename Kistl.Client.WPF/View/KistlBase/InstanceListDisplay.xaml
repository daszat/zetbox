<UserControl x:Class="Kistl.Client.WPF.View.KistlBase.InstanceListDisplay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:client="clr-namespace:Kistl.Client.WPF.View"
             xmlns:clientKistlBase="clr-namespace:Kistl.Client.WPF.View.KistlBase">
    <DockPanel>
        <GroupBox DockPanel.Dock="Top" Header="Filter" Padding="10">
            <StackPanel>
                <ItemsControl ItemsSource="{Binding FilterViewModels}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100" />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Label}" />
                                <ContentPresenter Grid.Column="1" Content="{Binding}" ContentTemplateSelector="{StaticResource defaultTemplateSelector}" />
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Label" />
                    <TextBox 
                         Grid.Column="1"
                         Text="{Binding InstancesSearchString, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=true}" />
                </Grid>
            </StackPanel>
        </GroupBox>

        <ToolBar DockPanel.Dock="Bottom">
            <Button Click="New_Click">New Item</Button>
            <Button Click="Open_Click">Open Item</Button>
            <Button Click="Edit_Click">Edit Class</Button>
            <Button Click="Refresh_Click">Refresh</Button>
        </ToolBar>

        <ListView x:Name="ClassList"
                  Margin="10"
                  BorderThickness="0"
                  ItemsSource="{Binding InstancesFiltered}"
                  ItemTemplateSelector="{StaticResource listItemTemplateSelector}">

            <ListView.Template>
                <ControlTemplate>
                    <ScrollViewer HorizontalScrollBarVisibility="Visible">
                        <StackPanel IsItemsHost="True" />
                    </ScrollViewer>
                </ControlTemplate>
            </ListView.Template>

            <ListView.Resources>
                <Style TargetType="ListViewItem">
                    <EventSetter Event="MouseDoubleClick"
                                 Handler="ObjectList_MouseDoubleClick" />
                </Style>
            </ListView.Resources>

        </ListView>

    </DockPanel>
</UserControl>
