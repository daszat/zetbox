<ctrls:WindowView x:Class="Kistl.Client.WPF.View.CacheDebuggerDisplay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctrls="clr-namespace:Kistl.Client.WPF.CustomControls;assembly=Kistl.Client.WPF.Toolkit"
    xmlns:commands="clr-namespace:Kistl.Client.WPF.Commands;assembly=Kistl.Client.WPF.Toolkit"
    commands:SmartRoutedUICommand.IsCommandSink="True"
    Title="Cache Debugger" Height="300" Width="300">
    <DockPanel>
        <Button Command="{Binding ClearCommand, Converter= {StaticResource CommandConverter}}" Content="Clear all Caches" DockPanel.Dock="Bottom" />
        <ListView ItemsSource="{Binding Caches}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <DockPanel HorizontalAlignment="Stretch">
                        <TextBlock DockPanel.Dock="Left" Text="{Binding ItemCount}" />
                        <TextBlock DockPanel.Dock="Left" Text="{Binding Name}" Margin="5,0,0,0" />
                    </DockPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </DockPanel>
</ctrls:WindowView>
