using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Kistl.API;

namespace Kistl.API.Client
{
    public static class ClientExtensionHelper
    {
        /*public static T GetObject<T>(this IDataObject obj, int ID) where T : class, IDataObject
        {
            IClientObject client = ClientObjectFactory.GetClientObject(new ObjectType(typeof(T).FullName));
            return (T)client.GetObjectGeneric(ID);
        }*/
    }
}
